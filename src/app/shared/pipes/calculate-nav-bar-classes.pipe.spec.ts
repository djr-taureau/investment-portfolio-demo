import { CalculateNavBarClassesPipe } from "./calculate-nav-bar-classes.pipe";

describe("CalculateNavBarClassesPipe", () => {
    it("create an instance", () => {
        const pipe = new CalculateNavBarClassesPipe();
        expect(pipe).toBeTruthy();
    });
});
